```
.
├── kitty.conf                # Main config file (should @include the others)
└── conf.d/
    ├── 00-theme.conf         # Colors, window decorations
    ├── 10-font.conf          # Font family, sizes
    ├── 20-shortcuts.conf     # Key mappings
    └── 30-ui.conf            # Window behavior, tab bar, etc.
```
